disableKinds = ['taxonomy', 'term']
baseURL = 'https://dmaivel.com/'
locale = 'en-us'
title = 'dmaivel'
[markup]
  [markup.highlight]
    style = "vs"
    noClasses=false
[markup.goldmark.renderer]
  unsafe = true
[markup.goldmark.extensions.passthrough]
  enable = true
[markup.goldmark.extensions.passthrough.delimiters]
  block = [['\[', '\]'], ['$$', '$$']]
  inline = [['\(', '\)'], ['$', '$']]
[markup.goldmark.parser]
  autoHeadingID = true
  autoHeadingIDType = "github"
[params]
  favicon = "./favicon.ico"
  htmlLang = 'en-us'
  description = 'I explore compilers, operating systems, virtualization, and runtimes.'
  ogLocale = 'en_US'
[permalinks]
  posts = "/:contentbasename/"
